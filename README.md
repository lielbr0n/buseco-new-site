# buseco-new-site
Install requirements:

Go to root directory of the app using CMD and type composer install.
Type and run on CMD php artisan migrate.
Type and run on CMD npm install.
To run the application:

Go to root directory of the app using CMD and type npm run dev.
Open separate CMD, go to root file and run php artisan serve.
